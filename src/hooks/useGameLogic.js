import { useState } from "react";
import { useEffect } from "react";

const useGameLogic = (cards) => {
  //Map the ID to the status of the card. ID is the url.
  cardStatus: (cards) => {};
};
export default useGameLogic;
